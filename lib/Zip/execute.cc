#include "Zip.ih"
void Zip::execute()
{
  // Zip all files in a folder, based existing order.txt in each folder.
  // If a line is prepended with -, make sure to ignore the - when considering
  // the file path.
}
