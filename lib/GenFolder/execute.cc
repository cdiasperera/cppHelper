#include "GenFolder.ih"
void GenFolder::execute()
{
  // Recursively copy files:
  // As we are recursively copying:
  //   If folder, copy if it doesn't exist
  //   If file, if it doesn't exist then copy and replace.
}
